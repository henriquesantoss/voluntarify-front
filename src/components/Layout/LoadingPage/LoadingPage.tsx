import { Flex } from 'components/Flex'
import Loading from 'components/Loading'

export const LoadingPage = () => (
  <Flex align='center' justify='center' fill='both'>
    <Loading size={200} />
  </Flex>
)
